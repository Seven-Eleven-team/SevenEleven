<%@ page pageEncoding="UTF-8" %>
<%@ taglib prefix="c" uri="jakarta.tags.core" %>
<%@ taglib prefix="fn" uri="jakarta.tags.functions" %>

<c:set var="isLogin"
       value="${not empty sessionScope.loginUser
               or not empty sessionScope.loginUserId
               or not empty sessionScope.userId
               or not empty sessionScope.nickname}" />

<header class="site-header">
    <button type="button" class="hamburger-btn" aria-label="메뉴 열기">
        <span></span>
        <span></span>
        <span></span>
    </button>

    <a href="${pageContext.request.contextPath}/" class="site-logo-link">
        <h1 class="site-logo">지출메이트</h1>
    </a>

    <div class="header-action-area ${isLogin ? 'is-logged-in' : 'is-logged-out'}"
         id="headerActionArea"
         data-server-login="${isLogin}">

        <a href="#"
           class="auth-link"
           id="headerAuthLink"
           data-auth-open="login">
            로그인 / 회원가입
        </a>

        <a href="${pageContext.request.contextPath}/mypage"
           class="user-profile-link"
           id="headerProfileLink"
           data-auth-required="true"
           aria-label="마이페이지로 이동">
            <span class="user-avatar" id="userAvatarInitial">
                <c:choose>
                    <c:when test="${not empty sessionScope.loginUser.nickname}">
                        ${fn:substring(sessionScope.loginUser.nickname, 0, 1)}
                    </c:when>
                    <c:when test="${not empty sessionScope.nickname}">
                        ${fn:substring(sessionScope.nickname, 0, 1)}
                    </c:when>
                    <c:otherwise>M</c:otherwise>
                </c:choose>
            </span>
        </a>
    </div>
</header>

<%@ include file="/WEB-INF/views/common/layout/sidebar.jspf" %>
<script>document.querySelector('.site-header')?.classList.add('is-solid');</script>